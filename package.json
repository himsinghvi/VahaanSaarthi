{
  "name": "vaahan-saarthi-full",
  "private": true,
  "version": "1.0.0",
  "description": "Vaahan Saarthi — single Vercel deployment (React + FastAPI)",
  "scripts": {
    "build": "npm run build --prefix frontend",
    "dev": "npm run dev --prefix frontend",
    "dev:backend": "python -m uvicorn app.main:app --reload --host 127.0.0.1 --port 8020"
  }
}
